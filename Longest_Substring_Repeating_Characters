/*Longest Substring Without Repeating Characters
Find the length of the longest substring characters. without repeating characters
Example:
Input: "abcabcbb"
Output: 3

abcabcbb
1 = 0
right3--> a
set() => (a,b,c)*/


import java.util.*;
public class Main {
	public static void main(String[]args) {
		Scanner sc=new Scanner(System.in);
		String l=sc.next();
		Set<Character> set=new HashSet<>();
		int left=0;
		int maxi=0;
		for(int i=0;i< l.length();i++){
		    while(set.contains(l.charAt(i))){
		        set.remove(l.charAt(i));
		            left++;
		    }
		    set.add(l.charAt(i));
            maxi= Math.max(maxi, i-left+1);
		}
		System.out.println(maxi);
	}

}

Output:
aabcabccb
3
